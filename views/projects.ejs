<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%= title %></title>

        <meta name="description" content="">
        <meta name="author" content="@濠梁钓鱼">
        
        <%-include('pre-header.ejs') %>
        <!-- specific css -->
        <link rel="stylesheet" href="/stylesheets/main.css">
        
        <style></style>
    </head>
    <body>
        <%-include('./nav/header-nav.ejs') %>
        
        <div class="container main_container projects">
            <div class="row">
                <% if(typeof projects !==  'undefined' && projects != null){ %>
                <% for(var i = 0; i < projects.length; ++i){ %>
                    <div class="metro">
                        <div class="metro_img"><a href="/projects/<%= projects[i].name %>" class="btn btn-link"><img src="/images/projects/<%=projects[i].name%>-logo.png" alt="logo"></a></div>
                        <div class="metro_info"><a href="/projects/<%= projects[i].name %>" class="btn btn-link"><%= projects[i].name %></a></div>
                    </div>
                <% }//for %>
                <% }//if %>
            </div><!-- row -->
        </div> <!-- container -->
        
<script>
</script>
        <%-include('footer.ejs') %>
        <%-include('post-footer.ejs') %>
<script>
</script>
    </body>
</html>